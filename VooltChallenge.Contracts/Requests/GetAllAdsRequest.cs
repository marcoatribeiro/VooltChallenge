namespace VooltChallenge.Contracts.Requests;

public sealed class GetAllAdsRequest
{
    // This request is empty because the requirements do not specify any parameters or filters for the GetAllAds endpoint.
    // However, if required, we could add properties to this class to support additional functionality in the future, such as filtering, sorting, paging, etc.
}
